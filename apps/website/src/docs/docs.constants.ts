import type { Locale } from '../i18n/i18n.constants';

// Docs translations can be enabled independently of the marketing website.
export const DOCS_LOCALES = ['en'] as const satisfies readonly Locale[];
